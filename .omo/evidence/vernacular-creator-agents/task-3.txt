## Task 3 — Database schema + CRUD — Evidence

### Files created
- `db/schema.sql` — PRAGMA journal_mode=WAL, 5 tables (brand_briefs, campaign_suggestions, conversations, contracts, dm_log)
- `database.py` — `Database` class with all CRUD functions from the plan

### Verification results
```
V1 ok: init_db succeeded
V2 ok: brief round-trip passed
V3 ok: conversation insert + update passed
V4 ok: get_conversation_details returns creator_username and raw_brief
V5 ok: get_todays_dm_count returns 0

All verifications passed.
```

### Notes
- `:memory:` DB sharing: `_connect()` caches the single connection for `:memory:` to avoid per-call empty-DB creation
- File-path DBs: each call opens a fresh connection; context-manager `with` handles commit/rollback
- Schema read from `db/schema.sql` relative to `database.py` at `init_db()` time
- Foreign keys enabled via `PRAGMA foreign_keys = ON` on every connection