## Task 1: Project Bootstrap — Evidence

### Directories (7/7)
tools, agents, prompts, db, data, tests, docs — all verified via Test-Path.

### .env.example keys (22/22)
AGENTS_DB_PATH, BUDGET_OVERRUN_PERCENT, DM_DELAY_JITTER, DM_DELAY_SECONDS,
FIREWORKS_API_KEY, FIREWORKS_BASE_URL, IG_PASSWORD, IG_SESSION_FILE, IG_USERNAME,
LOG_LEVEL, MAX_DMS_PER_DAY, MAX_NEGOTIATION_ROUNDS, MAX_PROFILE_FETCHES_PER_RUN,
MAX_TOKENS_PER_AGENT, MAX_TOTAL_TOKENS_PER_RUN, MODEL_CONTRACT, MODEL_DISCOVERY,
MODEL_NEGOTIATOR, MODEL_OUTREACH, MODEL_PROPOSAL, SCRAPER_API_URL, SCRAPER_DB_PATH

Verified via: python -c "from dotenv import dotenv_values; d=dotenv_values('.env.example'); print(sorted(d.keys()))"

### .gitignore patterns
Contains: .env, data/ig_session.json, __pycache__, data/*.db, .python-version, .omo/evidence/, .pytest_cache/

### requirements.txt
crewai>=1.15.0, instagrapi>=2.5.0, openai>=1.0, python-dotenv>=1.0, requests>=2.31, loguru>=0.7, pytest, pytest-mock
pip install --dry-run succeeded (resolved 90+ transitive deps).

### Scaffold-only
No Python logic files created. data/.gitkeep present. db/ directory empty.