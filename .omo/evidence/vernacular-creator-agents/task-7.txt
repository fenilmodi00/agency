# Task 7: Instagram DM/thread tools — Evidence

## File created
`tools/instagram_tools.py`

## Functions implemented
1. `send_instagram_dm(creator_username, message)` — resolves username to user_id, calls `get_ig_client().send_dm`, returns `{success, thread_id, error}`
2. `read_instagram_threads(amount=20)` — calls `get_ig_client().read_threads`, serialises thread objects to dicts
3. `read_thread_messages(thread_id, amount=50)` — calls `get_ig_client().read_thread`, serialises message objects to dicts
4. `get_profile(username)` — calls `get_ig_client().cl.user_info_from_username`, returns profile dict or None

## Constraint: No additional Client instances
All functions call `get_ig_client()` singleton — verified by inspection.

## Verification results

### 1. Import check
```
python -c "from tools.instagram_tools import send_instagram_dm; print(type(send_instagram_dm))"
<class 'function'>
```
✅ Import succeeds, type is function.

### 2. Monkeypatch — success path
Mock `get_ig_client().user_id_from_username` → 42, `send_dm` → `{success: True, thread_id: '123', error: None}`
Call `send_instagram_dm('alice', 'hi')` → result `success=True`, `thread_id='123'`
✅ PASSED

### 3. Monkeypatch — UserNotFound on resolution
Mock `user_id_from_username` to raise `UserNotFound`
Call `send_instagram_dm('alice', 'hi')` → result `success=False`, error contains "UserNotFound"
✅ PASSED

### 4. Monkeypatch — UserNotFound on send
Mock `user_id_from_username` → 99, `send_dm` to raise `UserNotFound`
Call `send_instagram_dm('bob', 'hey')` → result `success=False`, error contains "UserNotFound"
✅ PASSED